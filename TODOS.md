# TODOS

## Запланировано (backlog)

- [ ] **Категории рецептов + сортировка/фильтрация списка**
  - Что: разбивать рецепты по категориям (супы, мясо, рыба, десерты, гарниры,
    завтраки, выпечка…) и на главной уметь фильтровать по категории и сортировать
    (по дате, по алфавиту, недавно открытые).
  - Why: когда рецептов станет много, плоский список неудобен — искать долго.
  - Where to start:
    - Модель: добавить `category` в рецепт (в jsonb или отдельной колонкой).
      При генерации просить Gemini проставить категорию (перечень фиксированный,
      чтобы не расползалось; валидировать zod-энумом с фолбэком «Разное»).
    - Для старых рецептов — миграция «проставить категорию» (одноразовый проход
      через LLM или вручную в UI).
    - UI: чипы-фильтры категорий над списком + переключатель сортировки; состояние
      фильтра держать в URL (`?cat=…&sort=…`), чтобы шарилось и переживало обновление.
  - Depends on: ничего критичного; чисто аддитивно к текущему списку рецептов.

## Сделано

- [x] **Оффлайн для списка покупок** — service worker + localStorage; список и
  отметки купленного работают без сети. (Полное оффлайн-чтение всех рецептов —
  всё ещё можно расширить, но ключевой кейс «магазин без сети» закрыт.)
